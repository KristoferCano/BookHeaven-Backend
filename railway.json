{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && composer install --optimize-autoloader --no-dev"
  },
  "deploy": {
    "restartPolicyType": "always",
    "restartPolicyMaxRetries": 3
  }
}
